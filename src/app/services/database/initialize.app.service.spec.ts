import { InitializeAppService } from './database/initialize.app.service';

describe('InitializeAppService', () => {
  it('should create an instance', () => {
    expect(new InitializeAppService()).toBeTruthy();
  });
});
